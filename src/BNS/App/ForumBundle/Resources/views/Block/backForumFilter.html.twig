{% if forums|length > 0 %}

    {% for i,forumItem in forums %}
        {% if not group is defined or forumItem.group != group %}
            {% if group is defined %}
                </ul>
            </div>
        </div>
            {% endif %}
            {% set group = forumItem.group %}
        <div class="title-container">
            <div class="title border-light-blue {% if forumItem.group == forum.group %}active{% endif %}">
                {{ forumItem.group }}
                <span class="square icon-arrow"></span>
            </div>
            <div class="content-title" {% if forumItem.group != forum.group %}style="display:none"{% endif %}>
                <ul>
        {% endif %}

                <li><a {% if forumItem == forum %}class="active"{% endif %} href="{{ path('BNSAppForumBundle_back_slug', {"slug" : forumItem.slug}) }}">{{ forumItem.title }}</a></li>

    {% endfor %}
        {% if not group is defined %}
                </ul>
            </div>
        </div>
        {% endif %}

{% endif %}
