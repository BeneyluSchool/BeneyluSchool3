<li id="list_{{ category.id }}" data-toggle="menu" data-target="#category-edit-mode" class="category-edition">
	<div>
		<div class="loader loader-h"></div>
		<span class="list-grip"></span>
		<span class="category-icon gps-category-toggle {{ category.isActive ? "" : "off" }}"></span>
		<div class="title">{{ category.label }}</div>
		<span class="close-button">×</span>
	</div>
	<div class="category-editor">
		<input type="text" name="label" value="{{ category.label }}">
		<a href="#" class="btn bns-success btn-16 validate-small submit-category" data-toggle="menu" data-target="#default-header">
			<span class="icons-16 validate-icon"></span>
			<strong>Valider</strong>
		</a>
	</div>