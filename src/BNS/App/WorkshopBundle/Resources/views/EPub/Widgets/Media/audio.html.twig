<audio controls="controls">
  <source data-slug="{{ media.getSlug() }}" data-uid="{{ media.getUserId() }}" data-id="{{ media.getId() }}" src="{{ media.downloadUrl }}" type="{{ media.getHtml5AudioMimeType() }}" />
</audio>
