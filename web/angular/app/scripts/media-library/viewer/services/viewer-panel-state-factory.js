'use strict';

angular.module('bns.mediaLibrary.viewer.panelState', [])

  .factory('mediaLibraryViewerPanelState', function () {
    var srvc = {
      expanded: true,
    };

    return srvc;
  })

;
