(function (angular) {
'use strict';

angular.module('bns.embed.items', [])

  /**
   * @ngdoc constant
   * @name EMBED_ITEMS
   *
   * @description Array of valid embeddable items, dash-cased
   */
  .constant('EMBED_ITEMS', [
    'tour',
  ])

;

})(angular);
