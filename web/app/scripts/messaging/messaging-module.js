(function (angular) {
'use strict';

angular.module('bns.messaging', [
  // config
  'bns.messaging.config.states',
  // directives
  'bns.messaging.box',
  'bns.messaging.message',
]);

})(angular);
